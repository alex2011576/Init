# Use the output of the previous command to find the name and IP address of the
# device that makes the link between you (local network) and the outside world
#name: nat, ip: 10.60.1.1

# 'nat' stands for 'Network Address Translation'. nat is the device that makes the link between local network and outside world
traceroute who.int | grep nat
